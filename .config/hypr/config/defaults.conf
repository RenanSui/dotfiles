# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                     Defaults Configuration                  ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛


# General
$filemanager = 
$applauncher = wofi
$terminal = kitty
$idlehandler = swayidle -w timeout 300 'swaylock -f -c 000000' before-sleep 'swaylock -f -c 000000'

# Screenshots - grimblast is messed up right now 26th sep 2025
#$shot-region = grimblast copy area
#$shot-window = grimblast copy active
#$shot-screen = grimblast copy output

# Screenshots using grim + slurp
# $shot-region = grim -g "$(slurp -w 0)" - | wl-copy
# $shot-screen = grim - | wl-copy

# Screenshots using HyprQuickFrame + grim + slurp
$shot-region = quickshell -c HyprQuickFrame -n
$shot-screen = grim - | wl-copy